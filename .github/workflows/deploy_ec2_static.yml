name: Deploy sample.html to EC2 (Ubuntu + Nginx)

on:
  push:
    branches:
      - Deploy/Static_HTML
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key.pem
          chmod 600 ~/.ssh/ec2_key.pem

      - name: Add EC2 Host to known_hosts
        run: |  
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy sample.html to EC2 nginx root
        run: |
          scp -i ~/.ssh/ec2_key.pem sample.html ubuntu@${{ secrets.EC2_HOST }}:/tmp/sample.html

          ssh -i ~/.ssh/ec2_key.pem ubuntu@${{ secrets.EC2_HOST }} "
            sudo mv /tmp/sample.html /var/www/html/sample.html &&
            sudo systemctl restart nginx
          "